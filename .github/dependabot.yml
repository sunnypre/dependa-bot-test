version: 2
updates:
  - package-ecosystem: "nuget"
    directory: "src/backend/webapp"
    schedule:
      interval: "daily"
    open-pull-requests-limit: 5

  - package-ecosystem: "npm"
    directory: "src/client/test-client"
    schedule:
      interval: "daily"
    open-pull-requests-limit: 5


#Kommentarer:
#Options to control which dependencies are updated: allow, groups, ignore, vendor.
#Options to add metadata to pull requests: reviewers, assignees, labels, milestone.
#Options to change the behavior of the pull requests: target-branch, versioning-strategy, registries  för privata registries
